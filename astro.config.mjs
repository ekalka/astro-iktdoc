// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
    site: 'https://ekalka.github.io/astro-iktdoc',
    base: '/astro-iktdoc',
})
